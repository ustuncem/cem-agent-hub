---
name: typescript-naming-interfaces
description: >
  Interface naming for TypeScript contracts. Use when naming a new interface,
  reviewing existing interface names, or choosing between interface and type
  for an object shape.
---

# TypeScript Naming Interfaces

Placeholder skill for naming TypeScript interfaces as **contracts**.

Not yet a functioning skill — replace this body with real rules when authoring
the actual conventions (PascalCase shapes, no `I` prefix, props/callback/
generic patterns, `interface` vs `type`).

## Structure

- `SKILL.md` (required) — metadata + instructions
- `references/` (optional) — edge cases and extended examples, loaded on demand

## When to use

Replace with real trigger conditions: naming a new interface, reviewing names,
choosing `interface` vs `type`.
